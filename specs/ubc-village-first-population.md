# UBC village population — implementation assignment

User authorizes using existing UBC bodies and UAL animation library to make Meadow Harbor feel alive now; identical people are acceptable, garments come later. Grok CLI implements mechanical work. Primary Codex owns all final visual/spatial judgements, route acceptance, source diff review, and browser verification. This supersedes older instructions restricting implementation to another model.

Read `.tmp/grok-villagers-discovery.md`, CLAUDE.md, and the current completion GATES. You are not alone: preserve other edits. No git mutations, deployment, Sites operations, new packages, external communications, generation, or changes to src/sim. No browser/Blender UI. Do not restart the existing 5281 server. Do not invent visual acceptance from unit tests.

Ownership: `art/meadow-harbor-rebuild/completion/villagers.js` (small option to preserve only stationary background groups), new `villagers-ubc.js`, new `villager-routes.js`, focused `tests/villager-routes.test.mjs`, asset under `public/assets/ubc/`, and surgical integration in `main.js`. A small reproducible asset prep/check script under completion/scripts is permitted. Do not edit terrain, foliage, buildings, maritime, crops, lighting/camera config, unrelated tests, or deployment mirrors. Primary will review main.js diff before acceptance. Save small original copies of changed files in .tmp/ubc-village-baseline/ before edits.

Outcome: local village defaults to 12 independently animated UBC residents with six walkers, four people in two talking pairs, one idle/gesturing observer, and one kneeling worker. Replace the old ground-level screen-row crowd with these; retain the existing eight static seated people, six dock people and two elevated workshop people for this first population pass. Do not retain the removed static people invisibly costing draw calls. Clearly report retained background count. User accepts identical models, use only the existing male `art/universal-base-characters/assets/ubc-male-ual.glb`; no garment assets.

Primary's spatial direction:
- Reuse real world XY paths in `public/assets/terrain-layout-r15.json`, not the screen-space config placeholders. Six walkers: two per route, using `lower_loop`, `market_west`, and the lower portion of `western_lane` (vertices 2 through 5). Stay on clear path bands, off docks/water, farm plots, structures, fire, benches/tables, and rocks. Gentle terrain first; reject slopes above ~12 degrees for the initial routes so absence of foot IK is not hidden. Character horizontal radius 0.32 m; do not pretend this is full physics.
- Default adult standing height 1.78 m with feet planted via actual bind floor, consistent world scale (no screen-sized body distortion). Use front orientation +Z only after checking the source. Ground XY maps to world (x,height,-y).
- Initial social candidates: use old normalized screen-row anchors around (.570,.450) and (.416,.491) as pair centres, each with ~1.25 m face-to-face separation. Observer near (.553,.356). Worker near workshop approach endpoint, on clear ground beside ground timber rather than on scaffolding. Resolve screen anchors once at initialization; output world coords and rejected-clearance reasons for primary adjustment. Keep social people out of the walking lane. No unreviewed random relocation: expose candidates and validity, choose small deterministic local offsets only within 1.5 m of the anchor if needed.
- Simple purposeful cadence: walk, decelerate to idle at ends, turn while stationary, reverse; varied starting phases and pauses, no teleporting or instantaneous 180-degree flips. Match playback rate to actual travel speed (~0.9–1.15 m/s initial band) and stop walk animation while stopped. Social groups face one another with staggered Idle_Talking_Loop/Idle_Loop transitions. Worker alternates Fixing_Kneeling and a compatible stationary phase; preserve intentional root vertical movement, avoid a stand snap.

Mechanical requirements:
1. Load once and clone with SkeletonUtils; share immutable geometries/materials/textures/clips and give each actor independent bones/mixer. Never batchStaticModel a skinned mesh. Fail visibly on missing expected assets/clips; no placeholder-success fallback. Preserve source GLB. Prefer a reproducible derived asset retaining only required clips, reducing embedded images to <=1024 where readily supported by installed tools, and pruning unused binary safely. Do not spend the task building a custom asset optimizer: a single shared original 22.6MB GLB is acceptable if optimization is not straightforward; report it.
2. Implement small pure route/clearance helpers. Use graded-site rotated footprints with padding, plot/fence/prop bounds where available, inland/height checks, and scene-derived solid object bounds where they can be identified unambiguously. Avoid giant terrain/canopy bounding boxes. Root must see diagnostics for any unhandled obstacles. Validate segments at <=0.25m spacing and reject unsafe portions; never silently send actors through blockers. For walking paths, clearance width and gentler slope are requirements; if a full path is not valid, report and choose the longest valid contiguous segment of >=4m on that same named path. Preserve authored path band when smoothing corners. Separate walkers using direction-appropriate small lane offsets where width permits and yielding/spacing when necessary; no actor intersection. Do not add a full navmesh/Rapier engine.
3. Integrate residents.update(t,dt) in rendering; keep same timebase for world movement and animation. Correctly evaluate still frames and initial pose. Retain existing hidden-page and idle rendering behavior, and ensure default live view actually animates. No per-frame static world traversal or unnecessary allocations. Account for current frozen shadow map: newly animated characters must not leave frozen person-shaped shadows; use an appropriate bounded dynamic/contact shadow approach without rebaking all vegetation every frame or changing global appearance.
4. Add DEV-only resident diagnostics and deterministic sample/seek/follow-camera hooks for primary browser QA. Expose actual clip times, world positions, facing, terrain heights, route distances/validity, actor separation and counts, plus no spring/cloth claims. Helpers must evaluate requested poses correctly even while paused. Extend the existing debug API, not a visible admin panel. A small user-facing pause/resume control consistent with existing navigation is acceptable if needed for comfortably inspecting live villagers.
5. Focused meaningful tests: unsafe obstacle crossing rejected, coastline/slope exclusion, path endpoints/turn continuity, and separation/speed/phase logic. Node-based asset smoke validates clips and independent skeletons if feasible. Run village build and existing farm/harbor tests. Redirect logs; report commands and outcomes compactly. No broad root app suite for this isolated village feature.

Deliver one reviewable working pass, not an open-ended polish loop. Write `.tmp/grok-villagers-implementation.md` with files changed, commands, asset size, route/spatial diagnostics, how primary can use debug review controls and known limitations. Stop after the build and relevant checks, leaving the server available. Primary will inspect the real villagers before giving revisions.
